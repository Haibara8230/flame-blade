# 🔥 炎之刃：不熄之约 · FLAME BLADE

> 一款**热血日漫风格**的「文字 + 图画」RPG 冒险游戏。
> 纯前端实现（HTML5 Canvas + 原生 ES Module），**零依赖、可离线**运行。
> 所有美术（人物立绘、场景、战斗特效、精灵动画）都是**代码程序化绘制**的，没有任何外部图片素材。

```
「我的剑，不会为仇恨而挥——它为守护而出鞘。」
```

---

## 🎮 如何开始

**方式一（推荐）**：双击 `启动游戏.cmd`
→ 自动在本机起一个静态服务器并打开浏览器。

**方式二（手动）**：
```powershell
cd game
node tools/serve.cjs 8123
# 浏览器打开 http://127.0.0.1:8123/
```

> ⚠️ 必须通过 http 打开。游戏使用 ES Module，直接双击 `index.html`（file://）会被浏览器的
> 跨域策略拦住。任何静态服务器都可以，例如 `python -m http.server 8123`。

**调试模式**：`http://127.0.0.1:8123/?debug=1` 会额外挂上自动化测试接口
（`__autoRun` / `__god` / `__state` 等），供 `tools/` 下的回归脚本使用；正常游玩不需要。

---

## 📱 手机上玩

**可以玩，而且专门做了手机适配。**

- **同一局域网**：在跑服务器的电脑上查内网 IP（`ipconfig`，形如 `192.168.1.7`），
  手机浏览器打开 `http://192.168.1.7:8123/`。首次点击屏幕会**自动尝试全屏**，
  也可点右上角 `⛶` 手动切换。
- **请横屏玩**：游戏是 16:9 横屏。竖屏时会弹出「请把手机横过来」提示页，
  横过来自动消失（无需刷新）。
- **手机端专属行为**：
  - 战斗全程只需要点指令按钮，**不需要任何时机操作**，也就不再有悬浮圆钮遮挡画面。
  - 一次触摸只算一次确认（修掉了触摸事件重复触发的问题）。
  - 对话框 / 指令栏 / 技能栏 / 商店按屏宽重排，超矮屏自动收起次要信息。
  - 禁用双击缩放、下拉刷新与页面回弹；兼容刘海屏安全区（`viewport-fit=cover`）。
  - 横屏按高度铺满并适度放大，但**不允许宽度溢出视口**——4:3 平板上溢出会把指令栏最右的按钮裁到屏幕外。
    非 16:9 的屏幕上宁可上下留黑边。
  > 注：下方截图 12~17 拍摄于旧版时机弹反系统，画面中的圆钮已随改版移除。
- **测试范围**：已用无头 Chrome 在 6 种手机/平板视口（iPhone 14 / Android / iPhone SE / iPad
  × 竖屏 / 横屏）+ 桌面 1280 下验证布局、触屏按键尺寸、点击推进剧情与触摸输入链路，
  全部通过且控制台无报错（见 `screenshots/12~17-*.png`）。
  **未在真机上实测。**
- **声音**：浏览器要求先有用户交互才允许播放，进游戏点一下屏幕即可。

---

## 🕹️ 操作

| 操作 | 按键 / 鼠标 |
| --- | --- |
| 推进对话 / 确认 | `Z` `Enter` `空格` 或 **直接点击画面** |
| 下达战斗指令 | 点击指令按钮（战斗中不需要任何按键操作） |
| 逃跑 | 指令栏的「逃跑」按钮——只在允许脱离的战斗里出现 |
| 打开队伍 / 道具 / 存档 | 右上角按钮 |
| 返回标题 | 右上角 `✦ 标题` |

### 战斗核心玩法

- **速度行动条**：全场单位（我方 + 敌方）共用一条行动条，`速度` 决定涨条快慢，
  谁先涨满谁出手。**速度高的单位能在慢速单位出手一次的间隔里行动两次**，
  画面顶部会显示接下来 8 个出手者。轮到谁就给谁下一条指令，一次只管一个角色。
- **推条 / 拉条 / 加速 / 迟缓**：行动条不是只能看，是可以打的——
  雷的「连突·四连枪」把目标的行动条往后打 22 点；苍的奥义让**全队立刻抢到先手**；
  凯的「焦炎·狮子奋迅」给自己挂加速（速度 ×1.6）；璃的「暴雪」有机会让全体敌人迟缓（×0.6）。
  敌方同样有：暴风雪会让你迟缓，终焉魔王的「终焉之刃」会把全队的条一起打回去。
  顶部的出手顺序条是实时重算的，条被动了立刻就能看出来。
- **资源：术力 / 怒气**：技能和奥义统一消耗资源，但**不同角色的资源来源不同**——
  苍 / 雷 / 璃 用**术力**（开战满值，每次自己出手自然回复）；
  凯 用**怒气**（开战为 0，靠攻击命中、挨打、格挡、弹反积攒，越打越燃）。
  奥义不再需要单独的槽，攒够资源即可发动，仍带专属 cut-in 演出。
- **格挡 / 弹反（概率）**：敌人每次命中会先按你的属性滚判定——
  **弹反**（伤害全免）→ **格挡**（伤害 ×0.38）→ 全额承受。
  概率来自角色基础值 + 装备（锁子甲、魔铠加格挡，疾风之靴加弹反）。
  选择**「格挡」指令**可让该角色在下次出手前大幅提升这两个概率，并额外积攒怒气。
- **属性克制**：炎 / 冰 / 雷 / 暗 / 圣。打中敌人弱点有 **1.5 倍** 伤害并弹出「弱点！」。
  魔性的首领怕圣属性——苍的「圣光·裁罪」就是为它们准备的。
- **连击多段**：连刃三连斩、四连枪、奥义五段斩等，每一段独立结算暴击与特效。
- **首领二阶段**：Boss 血量降到一半会**变身**，攻击力上升，头像血条泛红。
- **状态异常**：剧毒、灼烧、冰封、眩晕、封印、迟缓、再生、攻击↑、防御↑、加速 等。
  持续时间按**该单位自己的出手次数**计算。
  **封印**会让目标只能普攻 / 格挡 / 用道具（技能栏会禁用并说明原因）。

---

## 📖 故事

### 第一部 · 不熄之约（序章 + 六章 + 终章）

| 章节 | 舞台 | 内容 |
| --- | --- | --- |
| 序章 | 灰烬之村 | 练习战、送刀送药、钟楼与红绳之约 |
| 第一章 | 燃烧的村庄 | 分工救援、父亲牺牲、追踪小铃的马车 |
| 第二章 | 黑之森 | 林中空营地的线索，结识苍与雷，净化守卫 |
| 第三章 | 港町 | 与小铃重逢，揭穿贩卖难民的商会，守住码头撤离 |
| 第四章 | 极北之城 | 接纳璃，帮助避难所，对抗丝薇雅，修复灯塔 |
| 第五章 | 封印遗迹 | 阅读旧日记忆，面对父亲的过去 |
| 第六章 | 深渊魔城 | 破解影廊，救出囚犯，对抗古兰与白雪 |
| 终章 | 终焉王座 | 击破核心、完成封门，兑现旅途中结下的支援 |

- **TRUE END · 一起回家**：读完封门术式，最终选择四人共同承担。
- **NORMAL END · 雪后的信**：使用维持封印的方法，璃留下，其他人继续寻找接她回来的路。
- **BAD END · 无声之白**：战败后选择观看失败结局；也可以恢复战前状态重试。
- **SECRET END · 世界的回声**：共同封门，并已取得森林、港町船队、北境灯塔三地支援。

### 第二部 · 门的另一边（第七章 ~ 终幕）

门关上了，但门后面并不是空的。**TRUE / SECRET 结局会接进第二部**——
只有把所有人都带回来的人，才有资格回头去问「那门后面被关着的，又是谁」。

| 章节 | 等级 | 舞台 | 掉落品质 |
| --- | --- | --- | --- |
| 第七章 · 门的另一边 | 20~27 | 仙灵之野，四位守门人 | 仙灵 |
| 第八章 · 断天之径 | 27~34 | 用被删掉的人的名字铸成的禁制 | 天绝 |
| 第九章 · 神域七柱 | 34~42 | 七柱神，可任意顺序挑战 | 神器 |
| 终幕 · 不熄之约 | 42~50 | 时序、因果，以及「安静下来」这件事本身 | 超神器 |

- **FINAL END · 还能再吵一会儿**：终焉退去，世界停止褪色。

全流程 **40 场战斗**，等级上限 50。通关后可开启**二周目**，继承等级、天赋、遗物与装备。

---

## ✨ 特色系统

### 战斗

- **行动条**：速度决定出手顺序，技能可以推条/拉条；顶部实时显示接下来的出手序列。
- **预判弹反**：下达【格挡】后进入架势，弹反成功则免伤 + 反击 + 把对方行动条打回去，
  并获得大量怒气。主角的循环是「接下你这一刀，再还回去」。
- **怒气三段解放**：凯的怒气分 25 / 60 / 100 三档，分别对应不同强度的解放技，
  攒高档要一直挨打，有翻车风险也有更大回报。
- **弱点击破**：打中弱点累积击破槽，满槽触发 **BREAK**——眩晕一回合、行动条清零、
  两回合内受伤 +50%。属性选择第一次成为真决策。
- **首领阶段**：首领在血量阈值换台词、换数值、换技能表，终焉有四个形态。
- **连携技**：行动条上相邻的两人 + 羁绊达标即可合击；四人连携【不熄之约】需要全员羁绊 Lv4。
- **目标战**：净化 / 护送 / 封门三类特殊胜利条件。
- **倍速与自动**：指令栏可切换 ×1 / ×2 / ×3，以及自动战斗。

### 养成

- **属性点**：每级 3 点，分配到 力 / 体 / 敏 / 念，各自影响两项派生属性。
- **天赋树**：每个角色三条分支、各四层，同一分支要点满上一层才能往下走。
  分支决定流派（凯的炎道 / 守势 / 不退，璃的冰核 / 术理 / 同行……）。
- **洗点**：营地里可随时退回全部已投入的点，鼓励试不同流派。
- **羁绊**：0~5 级。营地对话、战斗救人、支线、关键抉择都会积累，
  解锁连携技并影响结局。
- **遗物**：剧本里出现过的物件（两道结的红绳、刻着名字的树皮、折歪的纸太阳……）
  会成为可收集的遗物，各带一段回忆和一点全队加成。

### 装备

- 装备名称、品质与主题来自 `equipment-catalog.json`（**512 件**，8 品质 × 8 主题 × 8 件）。
- 实际数值由 `js/loot.js` 派生：**品质**决定数值档位与词缀条数，**主题**决定属性倾向
  （赤矿火纹偏炎、北地寒铁偏冰、游猎疾风偏速），**类别**决定基础曲线。
- **30 余种词缀**带真实战斗效果：吸血、荆棘、怒涛、破势、贯穿、先手、余响、不倒、还礼、
  虚影、时序……全部在 `battle.js` 里有对应实现。
- 每场战斗都可能掉落，**首领必掉**，且走它自己的主题（打古兰掉「赤狱战场」系）。
- 第二部的商店没有固定货架，按队伍等级现场生成。

> 换一份 `equipment-catalog.json` 后跑一次生成脚本即可整体替换命名，
> `js/equipment-data.js` 是生成产物，不要手改。

### 演出与表现

- **立绘**：程序化 SVG。骨架（脸型 / 眼型 / 瞳距 / 眉粗）决定「这是谁」，
  **六种表情差分**（normal / angry / hurt / cry / shout / smile）随台词切换。
  14 个角色 × 6 表情 = 84 张。旁白与系统提示会把立绘收起来，而不是显示一张灰掉的主角。
- **技能特效**：每个技能有自己的演出——三连斩画三道刀光、千鸟突从天上劈下来、
  绝对零度冻整个画面、狮子奋迅是脚下炎环加火柱。
- **BGM**：程序化音序器，和弦层 + 低音层 + 打击层 + 旋律层，四小节一段、A/B 段交替；
  首领进入新形态时切到变奏。仍然是纯 WebAudio，没有任何外部音频文件。

### 其它

- **营地**：每章之间的据点——休整（付费回满）、同伴对话（涨羁绊）、养成调整、补给。
- **战后回复固定两成**，不再自动回满，补给与休整因此需要规划。
- **图鉴**：击破过的敌人会记录弱点与数值。
- **存档**：4 个槽位（0 号自动存档 + 3 个手动），场景切换时自动写入。
- **结算板与二周目**：通关后列出走过的路与**错过的东西**，并可继承养成开始下一周目。

---

## 🗂️ 项目结构


```
game/
├── index.html            入口（Canvas + 所有 UI 层）
├── css/style.css         日漫风 UI（对话框 / 指令栏 / 抉择 / 奥义 cut-in / 标题）
├── js/
│   ├── main.js           主循环、场景推进、UI、商店、存档、音效
│   ├── battle.js         行动条调度、资源收支、概率格挡、奥义演出、连击、状态机
│   ├── story.js          剧情入口与旧版场景映射
│   ├── chapter-one.js    序章、灰烬之村
│   ├── chapter-journey.js 森林、港町、极北之城
│   ├── chapter-finale.js 遗迹、魔城、终战与四种结局
│   ├── story-helpers.js  场景、战斗、线索段落的构造工具
│   ├── chapter-beyond.js 第二部：仙灵之野、断天之径、神域七柱、终幕
│   ├── characters.js     角色 / 技能 / 连携 / 敌人 / 道具 / 装备 / 成长曲线
│   ├── growth.js         属性点与天赋树
│   ├── bonds.js          羁绊等级与连携解锁
│   ├── relics.js         遗物：回忆文本与全队加成
│   ├── loot.js           战利品：品质 / 主题 / 词缀 → 实际装备
│   ├── equipment-data.js 由 equipment-catalog.json 生成（勿手改）
│   ├── sprites.js        Canvas 程序化绘制：场景、人形精灵、魔兽、魔王、特效
│   └── portraits.js      SVG 程序化立绘：骨架 + 六种表情差分
├── equipment-catalog.json  512 件装备命名图鉴（策划稿）
├── 装备命名图鉴.md          同上的可读版本
├── screenshots/          实际运行截图（标题 / 对话 / 战斗 / 奥义 / 四种结局）
├── tools/
│   ├── serve.cjs         极简静态服务器
│   ├── validate.mjs      剧情图 / 数据完整性校验 + 成长曲线模拟
│   ├── reach.mjs         纯计算：哪些场景可达、哪些商店会开、玩家能拿到什么
│   ├── balance.mjs       战斗数值平衡模拟（按出手频率加权的轮数 / 生存力）
│   ├── battlesim.mjs     无浏览器战斗回归：查卡死、奥义可达性、推条/拉条是否生效
│   ├── playthrough.cjs   无头浏览器全流程通关回归测试
│   ├── mobile.cjs        手机/平板多视口适配验证（布局 + 触屏链路）
│   ├── shot.cjs          无头截图 + 控制台错误采集 + 步骤脚本执行
│   ├── png.cjs           截图像素分析（验证画面确实渲染出来了）
│   └── steps*.json       回归测试步骤脚本
├── play.ps1 / 启动游戏.cmd
└── package.json
```

---

## 🧪 自检工具

```powershell
cd game
node tools/validate.mjs     # 剧情图连通性、引用完整性、结局可达性、资源与弱点校验
node tools/balance.mjs      # 每场战斗的轮数与生存力评估
node tools/battlesim.mjs    # 真的跑完几场战斗，查行动条卡死与奥义可达性
node tools/playthrough.cjs  # 无头 Chrome 全流程通关（需先起服务器）
```

`validate.mjs` 会检查：
- 每个场景是否都有出路（不会卡死）、所有 `goto/join/item` 引用是否存在；
- 四种结局是否都可达；
- 每条台词长度、技能威力是否缺失；
- 每个角色的 `resource` 合法、奥义消耗不为 0 也不超出资源池、习得等级不高于主线终盘等级；
- 敌人的 `weak` 属性存在，且队伍里真的有人打得出这个属性；
- **可达性**：每个商店都被某个可达场景引用、每件装备/道具都能买到或被发放、
  每个状态都有技能会施加、每个属性都有攻击技能能打出
  （专防「台词说给了、实际没给」这种只有真人玩到才会发现的问题）；
- 首领强度按**实际遭遇等级**换算后是否单调递增（基准值之间没有可比性）；
- 模拟主线经验，确认终盘等级落在设计区间。

---

## 🎨 关于「素材」

题目里提到素材可以上网找。这里选择了**全部程序化生成**，原因是：

1. **零版权风险** —— 不引入任何第三方图片/音频；
2. **完全离线** —— 断网也能跑，不依赖 CDN；
3. **风格统一** —— 立绘、精灵、UI 用同一套配色（炎橙 × 暗紫 × 霜蓝），更像一部完整的日漫作品；
4. **可动画** —— 精灵是矢量路径，能实时做受击白闪、蓄力前倾、突进位移、倒地等形变。

如果你想换成真实素材，只需把 `sprites.js` 里的 `drawHumanoid / drawEnemy / drawBackground`
替换为 `ctx.drawImage(img, ...)`，并保留相同的 `pose` 参数即可。

---

## 📜 许可

个人学习 / 娱乐用途自便。
