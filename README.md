# 🔥 炎之刃 · FLAME BLADE

> 一款**热血日漫风格**的「文字 + 图画」RPG 冒险游戏。
> 纯前端实现（HTML5 Canvas + 原生 ES Module），**零依赖、可离线**运行。
> 所有美术（人物立绘、场景、战斗特效、精灵动画）都是**代码程序化绘制**的，没有任何外部图片素材。

```
「我的剑，不会为仇恨而挥——它为守护而出鞘。」
```

---

## 🎮 如何开始

**方式一（推荐）**：双击 `启动游戏.cmd`
→ 自动在本机起一个静态服务器并打开浏览器。

**方式二（手动）**：
```powershell
cd game
node tools/serve.cjs 8123
# 浏览器打开 http://127.0.0.1:8123/
```

> ⚠️ 必须通过 http 打开。游戏使用 ES Module，直接双击 `index.html`（file://）会被浏览器的
> 跨域策略拦住。任何静态服务器都可以，例如 `python -m http.server 8123`。

**调试模式**：`http://127.0.0.1:8123/?debug=1` 会额外挂上自动化测试接口
（`__autoRun` / `__god` / `__state` 等），供 `tools/` 下的回归脚本使用；正常游玩不需要。

---

## 📱 手机上玩

**可以玩，而且专门做了手机适配。**

- **同一局域网**：在跑服务器的电脑上查内网 IP（`ipconfig`，形如 `192.168.1.7`），
  手机浏览器打开 `http://192.168.1.7:8123/`。首次点击屏幕会**自动尝试全屏**，
  也可点右上角 `⛶` 手动切换。
- **请横屏玩**：游戏是 16:9 横屏。竖屏时会弹出「请把手机横过来」提示页，
  横过来自动消失（无需刷新）。
- **手机端专属行为**：
  - 战斗时右下角出现 **攻击 / 格挡** 两个 88px 大圆钮，位置避开对话框；平时推进剧情**直接点画面**即可。
  - 一次触摸只算一次确认（修掉了触摸事件重复触发的问题）。
  - 对话框 / 指令栏 / 技能栏 / 商店按屏宽重排，超矮屏自动收起次要信息。
  - 禁用双击缩放、下拉刷新与页面回弹；兼容刘海屏安全区（`viewport-fit=cover`）。
  - 手机横屏按高度铺满并适度放大，宁可左右轻微溢出也不让字号变得不可读。
- **测试范围**：已用无头 Chrome 在 6 种手机/平板视口（iPhone 14 / Android / iPhone SE / iPad
  × 竖屏 / 横屏）+ 桌面 1280 下验证布局、触屏按键尺寸、点击推进剧情与触摸输入链路，
  全部通过且控制台无报错（见 `screenshots/12~17-*.png`）。
  **未在真机上实测**，弹反时机的手感请以你自己设备为准。
- **声音**：浏览器要求先有用户交互才允许播放，进游戏点一下屏幕即可。

---

## 🕹️ 操作

| 操作 | 按键 / 鼠标 |
| --- | --- |
| 推进对话 / 确认 | `Z` `Enter` `空格` 或 **直接点击画面** |
| 格挡 · 弹反 | 敌人出招时按 `空格` 或 **点击画面**（手机点右下圆钮） |
| 打开队伍 / 道具 / 存档 | 右上角按钮 |
| 返回标题 | 右上角 `✦ 标题` |

### 战斗核心玩法

- **热血槽（0 → 100%）**：攻击、格挡、弹反都会积攒。**满 100% 即可发动【奥义】**
  （不消耗术力，带专属 cut-in 演出，屏幕速度线 + 抖动 + 白闪）。
- **格挡弹反**：敌人挥击命中前按下按键，进度条中央的**金色窄区**内命中 = **完美弹反**
  （伤害全免 + 热血暴增）；青色区域 = 普通格挡（伤害 ×0.38）。什么都不按就是硬吃。
- **属性克制**：火 / 冰 / 雷 / 暗 / 圣。打中敌人弱点有 **1.5 倍** 伤害并弹出「弱点！」。
- **连击多段**：连刃三连斩、四连枪、奥义五段斩等，每一段独立结算暴击与特效。
- **首领二阶段**：Boss 血量降到一半会**变身**，攻击力上升，头像血条泛红。
- **状态异常**：剧毒、灼烧、冰封、眩晕、防御↓、封印、再生、攻击↑ 等。

---

## 📖 故事（五章 · 四种结局）

| 章节 | 舞台 | 内容 |
| --- | --- | --- |
| 序章 + 第一章 | 灰烬之村 | 少年**凯**在燃烧的村庄失去父亲，第一次拔刀 |
| 第二章 | 黑之森 | 结识苍术士**苍**、猎人**雷**，净化被魔气侵蚀的森之守卫 |
| 第三章 | 极北之城 | 雪地里遇见魔王的女儿**璃**，取回圣剑·霜华 |
| 第四章 | 深渊魔城 | 四天王连战，璃的真相被揭开——**你的选择决定她的命运** |
| 第五章 | 终焉王座 | 与魔王决战，走向 TRUE / NORMAL / BAD / **SECRET** 四种结局 |

- **TRUE END**：在雪地里对璃伸出手、一路不放手。
- **NORMAL END**：松开了手，赢了战争，输了约定。
- **BAD END**：终焉之战倒下，世界被擦成一片白。
- **SECRET END（隐藏）**：终焉之战第一形态结束时**全队热血仍高于 55%**
  （即在战斗中大量使用弹反/攻击把热血顶住）→ 触发**限界突破**，解锁真·奥义。

---

## ✨ 特色系统

- **队伍**：凯（炎之剑士）/ 苍（苍蓝术士）/ 雷（疾风之枪）/ 璃（冰晶少女），各有独立立绘、成长曲线与技能树。
- **成长**：等级 + 经验 + 技能习得（按等级解锁）；**升级会立即回血回蓝**。
- **装备**：武器 / 护甲 / 饰品三槽位，商店购买后可在商店界面直接换装。
- **道具**：疗伤药、术力之泉、复苏之羽、封魔符、炎爆弹、遁形烟……
- **商店**：四个补给点（村庄 / 港町 / 北境 / 深渊前哨）。
- **存档**：右上角 `◇ 存档`（写入 localStorage），标题画面 `继续冒险` 读取。
- **音效与 BGM**：WebAudio 程序化合成（挥砍、暴击、弹反、升级、胜利小号、章节 BGM），无音频文件。

---

## 🗂️ 项目结构

```
game/
├── index.html            入口（Canvas + 所有 UI 层）
├── css/style.css         日漫风 UI（对话框 / 指令栏 / 抉择 / 奥义 cut-in / 标题）
├── js/
│   ├── main.js           主循环、场景推进、UI、商店、存档、音效
│   ├── battle.js         回合制战斗、格挡弹反、奥义演出、连击、状态机
│   ├── story.js          五章剧本（台词 / 分支 / 战斗 / 抉择 / 结局）
│   ├── characters.js     角色 / 技能 / 敌人 / 道具 / 装备 / 成长曲线
│   ├── sprites.js        Canvas 程序化绘制：场景、人形精灵、魔兽、魔王、特效
│   └── portraits.js      SVG 程序化生成日漫角色立绘（内联 data URI）
├── screenshots/          实际运行截图（标题 / 对话 / 战斗 / 奥义 / 四种结局）
├── tools/
│   ├── serve.cjs         极简静态服务器
│   ├── validate.mjs      剧情图 / 数据完整性校验 + 成长曲线模拟
│   ├── balance.mjs       战斗数值平衡模拟（回合数 / 生存力）
│   ├── playthrough.cjs   无头浏览器全流程通关回归测试
│   ├── mobile.cjs        手机/平板多视口适配验证（布局 + 触屏链路）
│   ├── shot.cjs          无头截图 + 控制台错误采集 + 步骤脚本执行
│   ├── png.cjs           截图像素分析（验证画面确实渲染出来了）
│   └── steps*.json       回归测试步骤脚本
├── play.ps1 / 启动游戏.cmd
└── package.json
```

---

## 🧪 自检工具

```powershell
cd game
node tools/validate.mjs     # 剧情图连通性、引用完整性、结局可达性、成长曲线
node tools/balance.mjs      # 每场战斗的回合数与生存力评估
node tools/playthrough.cjs  # 无头 Chrome 全流程通关（需先起服务器）
```

`validate.mjs` 会检查：
- 每个场景是否都有出路（不会卡死）、所有 `goto/join/item` 引用是否存在；
- 四种结局是否都可达；
- 每条台词长度、技能威力是否缺失；
- 模拟主线经验，确认终盘等级落在设计区间。

---

## 🎨 关于「素材」

题目里提到素材可以上网找。这里选择了**全部程序化生成**，原因是：

1. **零版权风险** —— 不引入任何第三方图片/音频；
2. **完全离线** —— 断网也能跑，不依赖 CDN；
3. **风格统一** —— 立绘、精灵、UI 用同一套配色（炎橙 × 暗紫 × 霜蓝），更像一部完整的日漫作品；
4. **可动画** —— 精灵是矢量路径，能实时做受击白闪、蓄力前倾、突进位移、倒地等形变。

如果你想换成真实素材，只需把 `sprites.js` 里的 `drawHumanoid / drawEnemy / drawBackground`
替换为 `ctx.drawImage(img, ...)`，并保留相同的 `pose` 参数即可。

---

## 📜 许可

个人学习 / 娱乐用途自便。
