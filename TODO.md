# 待办与已知问题

> 重排于 **2026-09-15**。
>
> 上一版 TODO 记录于 2026-09-11，**早于按原文重写剧情的那两次提交**（`1bd73ad` / `522409d`）。
> 那之后整个《炎之刃》流程与上一版原创虚构流程都退役了，
> 旧 TODO 里大量条目指向的内容已经不在游玩路径上，留着会淹掉真问题。
> 本次逐条复核：已修的移进「已完成」，因剧情重写而失效的移进「已作废」，
> 其余按当前真实状态重写。**凡写「已确认」的都在代码里核对过，或实跑过。**

---

## A. 缺陷（现有功能没按预期工作）

### A4. `validate.mjs` 的成长模拟仍是硬编码 ★最高优先

成长模拟用的是写死的经验值（`['战斗 第三章 2场', 220]`），不读 `SCENES` 里真实的敌人配置。
它现在推算并打印的是「凯 Lv.15」——**「凯」这个角色名在当前流程里已经不存在了**，
而真实结局是邪天 Lv6~9。

也就是说这一段输出**看起来像验证，实际上什么都没验证**，改敌人等级不会被它发现。

修法：把经验推导接到 `SCENES` + `ENEMIES[].exp`
（注意 `battle.js` 里 exp 会按 `1 + (lv-1)*0.22` 放大，且 `wild_wolf` 有 `baseLevel` 修正）。

> 已确认：2026-09-15 实跑，输出里仍是「凯 Lv.15」。

### A2. `weaponSkill` 定义了却基本没用

四个角色各配了 3~5 个技能 id，但全代码库只有一处读它
（`js/battle.js:911`，且只取第 0 个当普攻）。后面 2~4 个条目从未被任何代码引用。

设计意图看着像「武器决定可用技能」，做了一半。
要么补完（武器 → 解锁对应技能），要么删掉这个字段，别让它看起来像个已实现的系统。

> 已确认：`grep -rn weaponSkill js/` 只有 1 处读取点。上一版 TODO 记录以来未变。

### A3. `m.equips` 的槽位索引靠巧合对齐

`js/main.js:485`：

```js
const equips = ['weapon', 'armor', 'acc'].map(slot => defaultEquip(id, slot)).filter(Boolean);
```

`.filter(Boolean)` 把数组压缩了，之后 `openEquipScreen` 又按下标 0/1/2 当作武器/护甲/饰品用。
现在能对，仅仅因为四个角色的 `acc` 都是 `null` 且都有武器和护甲——
哪天加一个初始没武器的角色，护甲就会落到下标 0 被当成武器。

改成定长数组 `[weapon, armor, acc]`（空位保留 `null`），不要 filter。

> 已确认：2026-09-15 复核，代码未变。

### A11. `FORBIDDEN.fate` 与查证到的原文设定冲突

`js/realm.js` 把「命运之刻」做成一件 11 阶护符（`slot: 'acc'`，致命伤残血 + 预读敌方行动）。
但按原文笔记，**「永恒命运之刻」是这个世界的能量核心**——
它的遗失导致命运之塔封印溃散、八魔王逃出、魔气外溢，是全书主线，不是一件穿戴装备。

现在不影响游玩（11 阶永不掉落，剧情也没授予），但**剧情推进到第 11 章就会正面撞上**。
在那之前要决定：是把它从装备表里拿掉只当剧情物，还是保留装备形态并想清楚怎么自圆其说。

> 已确认：`refs/notes/vol1-001-008.md` 与 `js/realm.js` FORBIDDEN 段落对读。

### A6. 截图 12~17 已过期

`screenshots/12~17-*.png` 拍摄于旧版时机弹反系统，画面里还有已移除的触屏圆钮。
**且现在整个剧情都换了**，这批截图拍的内容已不存在。建议直接删掉重拍。

---

## B. 内容（剧情推进）

### B7. 原文笔记只做到第 8 章 ★

`refs/notes/` 下只有 `vol1-001-008.md`。要继续做第 9 章以后，
必须先按同样的格式补笔记（只记事实：谁在场、发生了什么、地名、等级、装备、战斗结果）。

原文已在 `refs/full.txt`，`tools/ingest.mjs` 已切好 1264 章到 `refs/chapters/`，
`refs/index.json` 是章节清单。**素材齐了，缺的是笔记这一步。**

下一段的目标节点：第 9~10 章 萝莉果果（**第一个队友，会解除单人限制**）
→ 第 11 章 永恒命运之刻 → 第 16 章 红叶盟。

### B9. 第三场（四只野狼）按模型会被秒，实战也确实死在这里 ★

A9 修好之后 `balance.mjs` 第一次给出可信结论，唯一被点名的就是 `d_fight2`：

```
八 野狼+野狼+野狼+野狼   我方HP 234  敌方HP 692  预计轮数 8.2  存活 1.4 轮  ⚠会被秒
```

两个独立来源都印证：`battlesim.mjs` 里 Lv3 单人打这场**打输**；
`playthrough.cjs` 的自动战斗也是死在这一带（全程死 3 次）。

不一定要改——原文这一段本来就是「0 级打 5 级怪等于找死」，
死几次再过可能正是想要的手感。但这是**目前唯一一处模型与实战同时报警的地方**，
要调就调这里。可选做法：四只改成三只、把第四只的等级降到 4、
或者在 `d_resupply` 之前再给一次补给。

> 已确认：2026-09-15，balance.mjs 与 battlesim.mjs 两处实跑。

### B8. 转职试炼一个都没实现

`trial_dragonsoul` / `trial_defysky` / `trial_goldendragon` 三个 flag 没有任何场景设置。
`canAdvance()` 因此永远返回 false，**转职在当前版本不可能发生**，
连带 6 阶以上装备的掉落闸门也永远关着。

按原文，邪龙系技能要到第 111 章才解锁——所以这不急，
但要注意**别再往「已实现」方向写文档**。

---

## C. 系统深度（新功能，按推荐程度排序）

这一节是上一版 TODO 里仍然成立的部分，剧情重写不影响它们。

### C4. 敌人 AI 是纯权重随机

`chooseEnemyAction()` 只做加权抽签，除了「Boss 半血变身」之外不看任何战场状态：
不会对残血目标集火、不会给自己上 buff、不会在我方有人濒死时补刀。
加一层条件判断（目标选择 + 技能选择）收益很大，改动集中在一个函数里。

### C5. 属性克制只有「弱点」，没有「抗性」

`weak` 只有 1.5 倍增伤一个方向。补 `resist`（×0.6）和 `immune`（×0），
配队和换技能才会产生真正的取舍。`computeDamage()` 里已有 `opt.weak` 的处理位置。

### C6. 技能树名不副实

现状是**按等级解锁的扁平列表**，到等级就自动全给，没有分支、没有取舍、没有技能点。
最小可行版本：保留等级解锁，在某几级插入「二选一」分支，选择存进存档。
改动集中在 `ACTORS.skills` 的数据结构 + 升级 UI，战斗代码不用动。

### C7. 状态异常没有抗性与命中修正

`inflict.chance` 是写死的固定值，首领和杂兵一样好控。
可以给敌人加 `statusResist`，并让命中率受等级差影响。
（天赋属性【专注力】已经接了异常抗性，但只作用于我方。）

### C8. 其他

- 没有难度选项。
- `B.log` 只保留最后 4 条（`addLog` 里 `if (B.log.length > 4) B.log.shift()`）。
  显示上够用，但写无头测试时事件会被截断——排查问题时容易误判成「功能没触发」。
- `tools/shot.cjs:120` 的 eval 错误仍只打印 `exceptionDetails.text`（恒为 `"Uncaught"`）。
  第 78 行的运行时异常已改用 `exception.description`，这一处漏了。
- `balance.mjs` 的模型按静态 `spd` 算出手频率，算不出 haste/slow 与推条拉条的价值，
  所以它对带这些效果的技能会系统性低估。

---

## D. 测试覆盖的盲区

### D7. 真实输入链路只覆盖了三条

A10 修好后，`mobile.cjs` 用真实 `PointerEvent` 覆盖的是：
剧情推进、抉择场景读完台词后按钮弹出、战斗中点「攻击」弹出目标列表。

**仍未覆盖**：技能 / 道具菜单、商店买卖、装备更换、存档读档的真实输入路径。
这些目前只有直接调内部函数的测试，点击链路上的判断错误一个都测不出来。

### D8. 只有一个结局，无从谈起多结局覆盖

当前只有 `vol1_ch8` 一个结局，所以上一版的「四个结局只测到一个」自动作废。
等剧情推进到有分支结局时，需要重新建立覆盖手段。

---

## 已完成

### 2026-09-14 ~ 15（按原文重写）

- **剧情整体按原文重建**：序章 ~ 第 8 章，40 个场景全部可达。
  作废了此前全部推测内容（「天痕」被当成人物、「普洛斯」被当成地名、
  四个虚构公会、苍梧区服等）。
- **数值体系换成原文三层结构**：自由属性 25 点 / 固定属性 10 点 / 天赋属性系统扫描，
  `realm.js` 为唯一事实源。幸运 0 → 永不暴击且伤害恒取下限，真实生效。
- **5 个死键修复**：`gaugePush` / `gaugePull`（引擎认的是 `gauge`，
  意味着此前所有「推条/拉条」技能描述都没生效过）、
  `fixedDamage` / `taunt` / `evadeUp` / `afterDodgeBonus`。
- **战斗从 1 场扩到 4 场**，按原文节奏分批；野狼数值改用原文的 170 血（加 `baseLevel` 修正）。
- **清掉三处假选项**（两个分支写的是同一件事）。
- `introLines` 播完后不交回控制权导致战斗卡死 —— 已移除该用法。
- **A1 换装备不更新格挡/弹反** —— `recalc()` 已补上 `blk` / `par` / `rageMul`（`js/main.js:538`）。
- **换装备只能从商店进入** —— 队伍面板已可进入装备界面（`js/main.js:1581`）。
- **文档回真** —— README.md / TODO.md 按实跑结果重写。
- **A7 `playthrough.cjs` 改测真实路径** —— 删掉开局强塞的三名队友，
  并加上断言：必须走到结局、队伍必须仍是单人、控制台必须零报错，失败时退出码非 0
  （此前无论结果如何都 exit 0）。
  实跑：邪天单人从 Lv1 起步，自动战斗中途死 3 次，Lv7 抵达结局，零报错。
- **A8 `battlesim.mjs` 重建** —— 崩溃的真实原因不是 `createBattle` 换了签名，
  而是它跑的 `c1_battle1` / `c3_battle1` / `c4_zain` / `c5_final` 四个场景在剧情重写时
  全被删了，`SCENES[id]` 返回 undefined，`createBattle` 取 `stage.bg` 时抛 TypeError。
  （上一版 TODO 把原因写成签名变更，是错的。）
  现在场次直接从 `SCENES` 推导，新增战斗不用改这个文件；队伍改为邪天单人，
  等级取自 playthrough 的真实记录；AI 按他现在这套无职业招式重写。
  技能机制回归也换成当前真实技能：投石拉条 / 读招 haste+回避 / 凝神 defUp+自愈 /
  擦身反手闪避加成，四项全绿，连跑三次稳定。
- **`selfHeal` 是个死键（battlesim 重建后抓到的）** —— 它只写在伤害循环里，
  `type:'buff'` 的技能永远走不到那一段，所以【凝神】描述写着「回复少量生命」、
  实际一点血都不回。已在 `js/battle.js` 的 buff 分支补上。
  这是继 `gaugePush`/`gaugePull`/`fixedDamage`/`taunt`/`evadeUp`/`afterDodgeBonus`
  之后的第七个同类问题——**技能描述承诺了、引擎没实现**，只有真打一场才看得见。
- **A9 `balance.mjs` 预设重建** —— 五档四人队（配 holy_sword / demon_mail 等
  《炎之刃》装备，脚本自己报 10 条「玩家拿不到」）换成三档邪天单人，
  等级取自真实通关记录、装备按「会去杂货摊花钱的玩家」估。
  档位改为按场景 id 选——四场狼战的敌人全是 5 级，按敌人等级根本区分不出第几场。
  顺带把「未被任何战斗使用的敌人」从 41 行名字压成一行（多数属已退役章节）。
  结论从「四场全部太简单/战力碾压」变成只点名 `d_fight2`，与实战一致（见 B9）。
- **A10 `mobile.cjs` 的过期用例** —— 三处 setup 全部换成当前真实内容，7 个视口恢复全绿：
  · 抉择场景 `c1_choice1`（已删除）→ `d_street`，并新增断言「场景必须真的切过去」，
    今后再删改场景会立刻报「测试用例已过期」，而不是静默假失败；
  · 点击次数不再写死 6 次，按场景实际行数推导上限、点到按钮弹出为止
    （实测打字机效果下一行要 3 次点击左右，写死必然算不准）；
  · 战斗 UI 探针不再用 `__advance(70)` 盲推（剧情一改就推不进战斗），
    改为直接起一场真实的狼战；四人队 + 退役敌人 `ice_hound` → 邪天单人 + `wild_wolf`。
  顺带发现 `js/main.js:922` 的 `escape: sc.escape !== false` 会让未声明 escape 的战斗
  多出一个「逃跑」按钮（指令按钮数 5 而非 4），探针已显式写 `escape:false` 与真实狼战一致。
  全局超时 280s → 420s（点击上限放宽后失败路径会跑更久）。

### 2026-09-11（分支 gameplay-optimization，部分成果仍在）

- `tools/reach.mjs` 可达性校验，validate 与 balance 共用。
- 推条 / 拉条：`haste` / `slow` 状态与技能的 `gauge` 字段。
- 战斗目标选择 `chooseTarget(m, kind, onPick, back)`：
  攻击 / 技能 / 道具 / 治疗 / 复活都能指定单位（此前治疗与复活永远作用在 `party[0]`）。
- `shot.cjs` 运行时异常改读 `exception.description`（eval 路径仍未改，见 C8）。

---

## 已作废（剧情重写后不再成立）

这些条目在 2026-09-11 时是真问题，现在指向的内容已经不在游玩路径上。
**保留记录是为了说明它们为什么消失，不是为了将来再做。**

| 旧条目 | 作废原因 |
| --- | --- |
| A5 冰之四天王·白雪从未登场 | 整个《炎之刃》敌人组已退役。现在 40+ 个敌人都「未登场」，不再是单条问题 |
| D1 圣剑·霜华玩家永远拿不到 | 该物品与第三章剧情一同退役 |
| D2「深渊前哨」商店从未开放 | 同上；当前唯一在用的商店是 `novice`（新手村杂货摊） |
| D3 逃跑功能整条链路不可达 | 当前四场战斗均 `escape: false`，设计上就不允许逃 |
| D4「封印」状态从未被施加 | 2026-09-11 已补接；且施加它的技能属退役内容 |
| D5「圣」属性没有技能能打出 | 2026-09-11 已补接（`characters.js` 现有 8 处 `elem: 'holy'`），但全在退役技能组里 |
| C1 三个抉择场景点不动 | 那三个场景（`c1_choice1` / `c3_meet` / `c4_reveal`）已删除。**测试本身的失效见 A10** |
| C2 自动 AI 打不赢终战 | 当前没有终战，只有一个结局 |
| B1 行动条缺少推条/拉条 | 已实现（见「已完成」）；但直到 `522409d` 才发现键名写错、此前从未生效 |
