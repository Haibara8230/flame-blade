# 待办与已知问题

> 记录于 2026-09-11。按「先修缺陷、再做系统」排序。
> 每条都标注了确认方式——凡是写「已确认」的都在代码里核对过，不是推测。

---

## A. 缺陷（现有功能没按预期工作）

### A1. 换装备不会更新格挡率 / 弹反率 ★最高优先

`js/main.js` 的 `recalc()` 复制了 `hp/mp/atk/def/spd/cri/mpRegen`，但**漏了 `blk` / `par` / `rageMul`**。
换装备和升级走的都是 `recalc()`，所以这三个值从角色创建那一刻起就锁死了。

后果：锁子甲 +6%、魔铠·黑曜 +10%、疾风之靴 +5% 弹反、羁绊之证 +4%/+3% —— **全部不生效**。
这正好打穿了 README 里写的「装备除了攻防还会影响格挡率/弹反率」。

> 已确认。是 2026-09-11 战斗重构引入的回归：`statsAt()` 新增了 blk/par，
> `makeMember()` 跟着复制了，但 `recalc()` 没有同步。

### A2. `weaponSkill` 定义了却基本没用

`ACTORS` 里四个角色各配了 4~5 个技能 id：

```js
kaito: weaponSkill: ['xinzhan', 'lianren', 'leiming', 'jiaoyan', 'miehun'],
```

但全代码库只有一处读它，且只取第 0 个当普攻（`js/battle.js`，`resolvePlayerCmd` 的 `case 'attack'`）。
后面 3~4 个条目从未被任何代码引用。看设计意图像是「武器决定可用技能」，做了一半。

要么补完（武器 → 解锁对应技能），要么删掉这个字段，别让它看起来像个已实现的系统。

> 已确认：`grep -rn weaponSkill js/` 只有 1 处读取点。

### A3. `m.equips` 的槽位索引靠巧合对齐

```js
const equips = ['weapon', 'armor', 'acc'].map(slot => defaultEquip(id, slot)).filter(Boolean);
```

`.filter(Boolean)` 把数组压缩了，之后 `openEquipScreen` 又按下标 0/1/2 当作武器/护甲/饰品用。
现在能对，仅仅因为四个角色的 `acc` 都是 `null` 且都有武器和护甲——
哪天加一个初始没武器的角色，护甲就会落到下标 0 被当成武器。

改成定长数组 `[weapon, armor, acc]`（空位保留 `null`），不要 filter。

> 已确认。

### A4. 经验曲线与实际遭遇对不上

`tools/validate.mjs` 的成长模拟用的是**硬编码经验值**（`['战斗 第三章 2场', 220]`），
不读 `SCENES` 里真实的敌人配置，所以它验证不了任何东西——改敌人等级也不会被它发现。

实测（`tools/playthrough.cjs` 全流程）队伍打到终战只有 **Lv12~13**，
而 validate 推算并断言的是 Lv15，差了 2~3 级。

修法：把经验推导接到 `SCENES` + `ENEMIES[].exp`（注意 `battle.js` 里 exp 会按 `1 + (lv-1)*0.22` 放大）。

> 已确认。这是重构之前就存在的问题。

### A5. 冰之四天王·白雪 从未登场

`ENEMIES.baixue` 有完整数据（HP2200 / ATK62 / 5 个技能 / 台词）、有立绘 `PORTRAITS.baixue`，
但 11 个战斗场景没有任何一个用到她——四天王实际只打了泽恩和古兰两个。

`tools/balance.mjs` 现在会把这类敌人报出来（`! 未被任何战斗使用的敌人`）。
要么给她加一场战斗（属剧情改动），要么删数据。

> 已确认。

### A6. 截图 12~17 已过期

`screenshots/12~17-*.png` 拍摄于旧版时机弹反系统，画面里还有已移除的触屏圆钮。
README 已加注说明，但没有重拍。

---

## B. 系统深度（新功能，按推荐程度排序）

### B1. 行动条缺少「推条 / 拉条」——最值得做 ★

行动条做出来了，但**玩家没有任何与之互动的手段**。
`STATUS` 里只有 `atkUp / defUp / regen / poison / burn / frozen / stun / defDown / seal`，
**一个影响速度的状态都没有**，战斗中 `spd` 完全静态。

这等于把阴阳师战斗的骨架搭好了，却没有它的策略核心：
主动加速自己、减速敌人、把敌人的条拉回去、把队友的条推上来、抢控制链。
现在「速度」只是一个决定出手频率的被动属性。

最小可行版本：
- `STATUS` 加 `haste` / `slow`，在 `effSpd()` 里生效（该函数已经支持 `buffs.spd`）；
- 技能效果加一类 `gauge: ±N`，直接加减目标的 `u.gauge`；
- 行动条预览（`drawTurnOrder`）本来就是实时重算的，会自动反映变化。

### B2. 敌人 AI 是纯权重随机

`chooseEnemyAction()` 只做加权抽签，除了「Boss 半血变身」之外不看任何战场状态：
不会对残血目标集火、不会给自己上 buff、不会在我方有人濒死时补刀、
不会在自己残血时后撤或治疗（敌人根本没有治疗技能）。

加一层条件判断（目标选择 + 技能选择）收益很大，改动集中在一个函数里。

### B3. 属性克制只有「弱点」，没有「抗性」

2026-09-11 补上的 `weak` 只有 1.5 倍增伤一个方向。
补 `resist`（×0.6）和 `immune`（×0），配队和换技能才会产生真正的取舍。
`computeDamage()` 里已有 `opt.weak` 的处理位置，扩展成本低。

### B4. 技能树（当前没有）

现状是**按等级解锁的扁平列表**，到等级就自动全给，没有分支、没有取舍、没有技能点，
每个角色每周目的成长完全相同。README 称之为「技能树」名不副实。

最小可行版本：保留等级解锁，在某几级插入「二选一」分支
（例：凯 Lv9 在 雷鸣·千鸟突 / 一个防御向技能 之间选一个），选择存进存档。
改动集中在 `ACTORS.skills` 的数据结构 + 升级 UI，战斗代码不用动。

### B5. 状态异常没有抗性与命中修正

`inflict.chance` 是写死的固定值，首领和杂兵一样好控。
可以给敌人加 `statusResist`，并让命中率受等级差影响。

### B6. 其他

- 没有难度选项。
- `B.log` 只保留最后 4 条（`addLog` 里 `if (B.log.length > 4) B.log.shift()`）。
  显示上够用，但写无头测试时事件会被截断——排查问题时容易误判成「功能没触发」。
- `tools/shot.cjs` 只打印 `exceptionDetails.text`（恒为 `"Uncaught"`），
  拿不到真实错误信息，调试时很费劲。应改为读 `exception.description`。

---

## C. 测试覆盖的盲区（2026-09-11 追加）

### C1. 自动化测试全部绕过输入层 —— 已部分补上

三个抉择场景点不动的 bug（玩家实际过不了第一章）能一直存在，是因为
**所有无头测试都直接调内部函数，从来没走过真实的输入链路**：

- `__advance()` 直接调 `nextLineCheck()`
- `__autoRun()` 对抉择场景直接调 `pickChoice(choices[0])`，连台词都跳过
- `__autoBattle()` 直接调 `doCmd()`

所以「点击 → 输入状态 → 推进」这条路上的判断错误一个都测不出来。

已在 `tools/mobile.cjs` 里补了一项：用真实 `PointerEvent` 连点 6 次走完
`c1_choice1`，断言台词读完且按钮弹出（并已反向验证该断言确实能抓到这个 bug）。

**仍未覆盖**：`c3_meet` / `c4_reveal` 只靠同一处代码保证；战斗指令、商店、
装备更换、存档读档都还没有真实输入路径的测试。

### C2. 自动 AI 打不赢终战，四个结局只测到一个

`tools/playthrough.cjs` 全流程跑下来稳定走到 **BAD END**——自动 AI 不会治疗、
不用道具、不会攒资源放奥义，终战必败。

后果是 TRUE / NORMAL / SECRET 三个结局**从来没有被端到端验证过**，
只有 `validate.mjs` 静态检查过「可达性」。

要真正覆盖，`__autoRun` 的 AI 需要会补血和用道具，或者提供一个
「强制指定结局路线」的测试钩子。

### C3. `tools/shot.cjs` 的错误信息不可用

只打印 `exceptionDetails.text`（恒为 `"Uncaught"`），真实错误在
`exceptionDetails.exception.description` 里。排查那个 `hideSubmenu` 未定义
的问题时，只能靠把错误写到 `window` 上再读回来。应该直接改掉。

---

## D. 定义了但玩家永远碰不到（2026-09-11 扫描发现，全部已核实）

这一类的共同点是：数据、图标、台词、甚至执行代码都写好了，但**没有任何一条路径
能让玩家真的用上**。用一段脚本比对「定义」与「可获得/可触发」得出，不是推测。

### D1. 圣剑·霜华 玩家永远拿不到 ★

`story.js:468` 写着 `['系统', '※ 璃 获得了【圣剑·霜华】！']`——但这只是**一行台词**，
不是 `{ item: 'holy_sword' }` 动作。`holy_sword` 不在任何商店、也没有任何场景发放。

游戏告诉你拿到了第三章的核心剧情道具，实际上背包里什么都没多。
（台词说给了「璃」，但 `holy_sword` 是剑，按 `defaultEquip` 的配置属于凯的武器类型，
这里本身也对不上。）

**连带影响数值平衡**：`tools/balance.mjs` 的 late / final 队伍预设给凯配的正是
`['holy_sword', 'demon_mail']`——两件玩家都拿不到的装备。凯 Lv15 的真实与模拟差距：

| | atk | def | hp | 格挡率 |
| --- | --- | --- | --- | --- |
| 实际可得（炎纹剑·绯 + 锁子甲） | 142 | 87 | 1034 | 0.24 |
| balance.mjs 假设 | 160 | 99 | 1094 | 0.28 |
| 差距 | **−11%** | **−12%** | −5% | −0.04 |

也就是说**终盘实战比模拟结论更难**——这大概也是 C2 里自动 AI 终战必败的一部分原因，
不能全归咎于 AI 弱。

### D2. 「深渊前哨 · 最后的交易」商店从未开放

`SHOPS.final` 定义了，但 `story.js` 里只有 `village` / `harbor` / `north` 三个
`shop:` 引用。它独家出售的 **魔铠·黑曜** 与 **羁绊之证** 因此也拿不到
（两者 `price: 0`，本来就是剧情道具性质）。

### D3. 逃跑功能整条链路都不可达

实现得很完整：`B.def.escape` 开关、`resolvePlayerCmd` 的 `case 'escape'`、
`attemptEscape()`、`G.onBattleEscape()`、道具 `遁形烟`。
剧本里 `c2_battle1` 还特意设了 `escape: true`，表示这场是允许逃的。

但是：
- **指令栏只有 攻击 / 技能 / 道具 / 格挡 四个按钮，没有「逃跑」**；
- **遁形烟 不在任何商店出售**，也没有场景发放。

所以两条入口都断了，`escape: true` 这个标记没有任何意义。

### D4. 「封印」状态从未被施加

`STATUS.seal`（✖ 封印，2 回合）有完整定义。但没有任何我方技能或敌方技能的
`inflict` 指向它——道具「封魔符」施加的是 `stun` 而不是 `seal`。

README 把「封印」列为状态异常之一。要么给某个技能接上，要么从 STATUS 和文档里删掉。

### D5. 「圣」属性没有任何技能能打出

`ELEM.holy` 定义了，`runSkill` 的 `isMagic` 判断里也算进了 holy，
但我方 20 个技能和敌方 19 个技能里**一个 holy 属性的攻击都没有**
（我方只有 none/fire/thunder/ice/dark，敌方 none/dark/ice/fire）。

README 宣传「属性克制：火 / 冰 / 雷 / 暗 / 圣」，圣是个幽灵。
我在补敌人 `weak` 时就是因为这个才刻意避开了 holy——否则那些弱点永远触发不了。

### D6. 建议：给 validate.mjs 加一层「可达性」校验

上面五条都是同一类问题，而且都能自动检测。建议加：
- 每件装备 / 道具是否至少能从某个**会开放的**商店买到，或被某个可达场景发放；
- 每个 `SHOPS` 条目是否真的被某个场景引用；
- 每个 `STATUS` 是否至少被一个技能 / 道具施加；
- 每个 `ELEM` 是否至少有一个攻击技能能打出；
- `balance.mjs` 的队伍预设只允许使用「玩家可获得」的装备。

这样才不会再出现「台词说给了、实际没给」这种只有真人玩到才会发现的问题。

---

## 已完成（2026-09-11，分支 gameplay-optimization）

- **A1 / D1 及一个新发现**：`recalc()` 漏了 blk/par/rageMul，且会抹掉剧情给的永久加成
  （第一章抉择承诺的「攻击力永久 +6」在下一次升级就没了，存档也没存）。
  圣剑·霜华原本只有一行台词说「获得了」，现已真正发放。
- **可达性校验**：新增 `tools/reach.mjs`，validate 与 balance 共用。
  一次抓出 8 条既有问题并全部补齐——D2 商店、D3 逃跑、D4 封印、D5 圣属性，
  外加两条我此前没发现的（`defUp` 与 `burn` 从未被任何技能施加）。
  balance.mjs 的队伍预设现在也不允许使用玩家拿不到的装备。
- **B1 推条 / 拉条**：新增 `haste` / `slow` 状态与技能的 `gauge` 字段，
  我方（四连枪拉条、苍的奥义全队推条、焦炎自加速、暴雪群体迟缓）与敌方
  （暴风雪迟缓、终焉之刃群体拉条）都有手段。`battlesim.mjs` 增加了对应回归。

### 仍然开着

A2（weaponSkill 只用第 0 个）、A3（equips 槽位靠巧合）、A4（经验曲线硬编码）、
A5（白雪未登场）、A6（截图过期）、
B2（敌人 AI 纯随机）、B3（只有弱点没抗性）、B4（技能树）、B5（状态无抗性）、B6、
C1（战斗指令/商店/装备/存档仍无真实输入测试）、C3。

**C2 已部分解决**：这一轮之后 `playthrough.cjs` 全流程第一次跑到了 **TRUE END**
（此前每次都是 BAD END——自动 AI 打不赢终战）。原因应该主要是补上的圣属性：
苍的「圣光·裁罪」对怕圣的魔王有 1.5 倍克制伤害，加上灼烧、迟缓与推条拉条，
终战的容错一下宽了很多。NORMAL / SECRET 两条线仍未端到端验证。

另新增两条待办：
- `balance.mjs` 的模型按静态 `spd` 算出手频率，算不出 haste/slow 与推条拉条的价值，
  所以它对带这些效果的技能会系统性低估（给泽恩加封术时就因此误报「太简单」）。
- 换装备只能从商店进入（`openEquipScreen` 仅被 `openShop` 调用），
  离开商店后无法调整装备。

---

## E. 战斗目标选择（2026-09-11 修复）

玩家报告「攻击、道具、治疗都没法选单位」。确认是功能缺失，不是操作问题——
原来三处目标全是硬编码：

```js
function pickTargetIdx() { ... return b.enemies.indexOf(alive[0]); }   // 永远第一个活着的敌人
const target = (s.target === 'ally' || s.target === 'party') ? 0 : pickTargetIdx();
doCmd(m, { type: 'item', item: k, target: it.target === 'enemy' ? pickTargetIdx() : 0 });
```

后果比表面更严重：
- 攻击无法集火残血敌人、无法挑弱点属性的目标；
- **疗伤 / 疗伤药 / 高级疗伤药 / 术力之泉 永远作用在 party[0]（凯）**，
  其余三人永远治不到；
- **复苏之羽 / 福音·复苏之光 也指向 party[0]**——凯活着时用它们是纯浪费，
  等于无法复活任何人。

这是「全队批量下令」时代留下的，改成逐个单位出手后目标选择本该是核心决策。

已补目标选择界面 `chooseTarget(m, kind, onPick, back)`：
- `enemy` 列出场上敌人，显示 HP% / 绝对值 / **弱点属性**（集火和挑属性需要的信息）；
- `ally` 列出同伴，倒下的禁用；
- `downed` 只允许选倒下的同伴（复活类技能与道具）；
- 群体 / 自身技能不弹窗；只剩一个合法目标时自动跳过，不多点一次；
- 「← 返回」回到上一级菜单。

`tools/mobile.cjs` 增加对应回归（点「攻击」后目标列表必须弹出，且项数等于
场上敌人数），并已反向验证——把目标选择退回硬编码时 7 个视口全部报错。
