# 待办与已知问题

> 记录于 2026-09-11。按「先修缺陷、再做系统」排序。
> 每条都标注了确认方式——凡是写「已确认」的都在代码里核对过，不是推测。

---

## A. 缺陷（现有功能没按预期工作）

### A1. 换装备不会更新格挡率 / 弹反率 ★最高优先

`js/main.js` 的 `recalc()` 复制了 `hp/mp/atk/def/spd/cri/mpRegen`，但**漏了 `blk` / `par` / `rageMul`**。
换装备和升级走的都是 `recalc()`，所以这三个值从角色创建那一刻起就锁死了。

后果：锁子甲 +6%、魔铠·黑曜 +10%、疾风之靴 +5% 弹反、羁绊之证 +4%/+3% —— **全部不生效**。
这正好打穿了 README 里写的「装备除了攻防还会影响格挡率/弹反率」。

> 已确认。是 2026-09-11 战斗重构引入的回归：`statsAt()` 新增了 blk/par，
> `makeMember()` 跟着复制了，但 `recalc()` 没有同步。

### A2. `weaponSkill` 定义了却基本没用

`ACTORS` 里四个角色各配了 4~5 个技能 id：

```js
kaito: weaponSkill: ['xinzhan', 'lianren', 'leiming', 'jiaoyan', 'miehun'],
```

但全代码库只有一处读它，且只取第 0 个当普攻（`js/battle.js`，`resolvePlayerCmd` 的 `case 'attack'`）。
后面 3~4 个条目从未被任何代码引用。看设计意图像是「武器决定可用技能」，做了一半。

要么补完（武器 → 解锁对应技能），要么删掉这个字段，别让它看起来像个已实现的系统。

> 已确认：`grep -rn weaponSkill js/` 只有 1 处读取点。

### A3. `m.equips` 的槽位索引靠巧合对齐

```js
const equips = ['weapon', 'armor', 'acc'].map(slot => defaultEquip(id, slot)).filter(Boolean);
```

`.filter(Boolean)` 把数组压缩了，之后 `openEquipScreen` 又按下标 0/1/2 当作武器/护甲/饰品用。
现在能对，仅仅因为四个角色的 `acc` 都是 `null` 且都有武器和护甲——
哪天加一个初始没武器的角色，护甲就会落到下标 0 被当成武器。

改成定长数组 `[weapon, armor, acc]`（空位保留 `null`），不要 filter。

> 已确认。

### A4. 经验曲线与实际遭遇对不上

`tools/validate.mjs` 的成长模拟用的是**硬编码经验值**（`['战斗 第三章 2场', 220]`），
不读 `SCENES` 里真实的敌人配置，所以它验证不了任何东西——改敌人等级也不会被它发现。

实测（`tools/playthrough.cjs` 全流程）队伍打到终战只有 **Lv12~13**，
而 validate 推算并断言的是 Lv15，差了 2~3 级。

修法：把经验推导接到 `SCENES` + `ENEMIES[].exp`（注意 `battle.js` 里 exp 会按 `1 + (lv-1)*0.22` 放大）。

> 已确认。这是重构之前就存在的问题。

### A5. 冰之四天王·白雪 从未登场

`ENEMIES.baixue` 有完整数据（HP2200 / ATK62 / 5 个技能 / 台词）、有立绘 `PORTRAITS.baixue`，
但 11 个战斗场景没有任何一个用到她——四天王实际只打了泽恩和古兰两个。

`tools/balance.mjs` 现在会把这类敌人报出来（`! 未被任何战斗使用的敌人`）。
要么给她加一场战斗（属剧情改动），要么删数据。

> 已确认。

### A6. 截图 12~17 已过期

`screenshots/12~17-*.png` 拍摄于旧版时机弹反系统，画面里还有已移除的触屏圆钮。
README 已加注说明，但没有重拍。

---

## B. 系统深度（新功能，按推荐程度排序）

### B1. 行动条缺少「推条 / 拉条」——最值得做 ★

行动条做出来了，但**玩家没有任何与之互动的手段**。
`STATUS` 里只有 `atkUp / defUp / regen / poison / burn / frozen / stun / defDown / seal`，
**一个影响速度的状态都没有**，战斗中 `spd` 完全静态。

这等于把阴阳师战斗的骨架搭好了，却没有它的策略核心：
主动加速自己、减速敌人、把敌人的条拉回去、把队友的条推上来、抢控制链。
现在「速度」只是一个决定出手频率的被动属性。

最小可行版本：
- `STATUS` 加 `haste` / `slow`，在 `effSpd()` 里生效（该函数已经支持 `buffs.spd`）；
- 技能效果加一类 `gauge: ±N`，直接加减目标的 `u.gauge`；
- 行动条预览（`drawTurnOrder`）本来就是实时重算的，会自动反映变化。

### B2. 敌人 AI 是纯权重随机

`chooseEnemyAction()` 只做加权抽签，除了「Boss 半血变身」之外不看任何战场状态：
不会对残血目标集火、不会给自己上 buff、不会在我方有人濒死时补刀、
不会在自己残血时后撤或治疗（敌人根本没有治疗技能）。

加一层条件判断（目标选择 + 技能选择）收益很大，改动集中在一个函数里。

### B3. 属性克制只有「弱点」，没有「抗性」

2026-09-11 补上的 `weak` 只有 1.5 倍增伤一个方向。
补 `resist`（×0.6）和 `immune`（×0），配队和换技能才会产生真正的取舍。
`computeDamage()` 里已有 `opt.weak` 的处理位置，扩展成本低。

### B4. 技能树（当前没有）

现状是**按等级解锁的扁平列表**，到等级就自动全给，没有分支、没有取舍、没有技能点，
每个角色每周目的成长完全相同。README 称之为「技能树」名不副实。

最小可行版本：保留等级解锁，在某几级插入「二选一」分支
（例：凯 Lv9 在 雷鸣·千鸟突 / 一个防御向技能 之间选一个），选择存进存档。
改动集中在 `ACTORS.skills` 的数据结构 + 升级 UI，战斗代码不用动。

### B5. 状态异常没有抗性与命中修正

`inflict.chance` 是写死的固定值，首领和杂兵一样好控。
可以给敌人加 `statusResist`，并让命中率受等级差影响。

### B6. 其他

- 没有难度选项。
- `B.log` 只保留最后 4 条（`addLog` 里 `if (B.log.length > 4) B.log.shift()`）。
  显示上够用，但写无头测试时事件会被截断——排查问题时容易误判成「功能没触发」。
- `tools/shot.cjs` 只打印 `exceptionDetails.text`（恒为 `"Uncaught"`），
  拿不到真实错误信息，调试时很费劲。应改为读 `exception.description`。
