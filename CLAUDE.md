# CLAUDE.md

本项目是《网游之邪龙逆天》（火星引力）的同人改编 RPG，个人自用，未公开发布。

---

## 最高原则：一切以原文为准

**改任何东西之前，先去 `refs/` 查原文。**

原文就在仓库里（`.gitignore`，不进版本库）：

| 路径 | 内容 |
| --- | --- |
| `refs/full.txt` | 全书 12 卷 1264 章 411 万字 |
| `refs/chapters/NNNN.txt` | 按章切好的正文，`0001.txt` = 序章 |
| `refs/index.json` | 章节清单（seq / 卷 / 章号 / 标题 / 字数） |
| `refs/notes/` | 已整理的结构化笔记 |

章号映射：`seq = 章号 + 1`（`0001` 序章、`0002` 第1章 …… `0009` 第8章）。

### 规则

1. **能和原文一模一样的，就照抄。** 数值、系统提示、装备名、任务名、
   等级奖励、怪物属性、货币单位、技能名——原文写了多少就是多少，
   不要「取整」「配平」「觉得这样手感更好」。
   例：原文写「野狼：5级，生命：170」，那就是 170，不是 165 也不是 180。
   例：原文写「叮……你的等级升为1级，生命+10，魔法+10，获得5点自由属性点。」
   ——系统提示就照这句写，不要改写成「※ 升级了！」。

2. **原文没写的，才轮到设计。** 并且必须在代码注释里标明「⚠ 原文未考证」，
   说清楚推测的依据。这个项目已经因为「看起来像已实现、其实是编的」返工过两次
   （详见 TODO.md 的「已作废」表）。

3. **原文和现有实现冲突时，改实现，不要改原文。**
   已经写好的代码不是保留它的理由。

4. **唯一允许偏离原文的地方是叙述文字的排版**：小说的叙述体塞进对话框读不了，
   必须拆成一句一句的台词和场景节拍。但**用词要尽可能贴着原文**，
   意思、语序、专有名词一律不动，不要自由发挥。

5. **查证过的事实写进 `refs/notes/`**，只记事实（谁在场、发生了什么、地名、
   等级、装备、数值、战斗结果），并在生成的代码文件头部注明依据哪份笔记。

### 反面教材（都真实发生过）

- 把「天痕」当成一个人物角色写了人设 —— 它是天空上一道百年前出现的白色裂痕。
- 把「普洛斯」当成地名 —— 它是天外集团总裁、《命运》主研发者。
- 把「永恒命运之刻」做成一件可穿戴护符 —— 它是这个世界的能量核心，全书主线。
- 自己发明了天阙/玄冥/落霞/九幽四个公会和整段公会战 —— 原著中不存在。
- 主角起始等级写成 1 级 —— 原文明写 **0 级**（「0级，身上只有没什么属性的新手衣」）。
- 给主角配了 5 个技能 —— 原文明写他**没有技能、没有职业**
  （「全世界只有叶天邪一个例外，他没有技能，没有职业」）。

---

## 战斗是「演出」，不是「玩法」

这个项目的目的是**体验原文**，不是做一个好玩的战斗系统。所以：

- 有原文对应的战斗，写成 **script 演出**（见 `js/chapter-destiny.js` 的 `d_fight1`），
  按原文的节拍一拍一拍播，玩家只看，界面上只留「倍速」和「跳过」。
- **不要逐回合模拟**。原文第9章那一场如果按回合算要 42 次出手，
  而原文自己把中段压成了「-12，-13，-14，-12，-12……」五个数字加一个省略号——
  照抄的是**节拍**，不是回合数。
- 伤害数字一律由引擎按真实属性现算（`computeDamage`），不要在剧本里写死。
  这样改了公式或装备，数字会自己跟着变，不会出现「剧本说 -15、面板算出来是 -80」。
- 不要给玩家加原文里没有的操作（时机闪避、连招、战术选择）。
  原文第9章他「连技能都没有使用过」，看点在他怎么赢，不在玩家的决策。

普通遭遇战（原文没有逐拍描写的）才走回合制引擎。

---

## 动手前的检查清单

```
1. 这次要改的东西，原文第几章写了？ → 读 refs/chapters/
2. 原文给了具体数值/原话吗？        → 给了就照抄，不要改
3. 原文没写的部分？                 → 注释里标「⚠ 原文未考证」+ 推测依据
4. 和现有实现冲突？                 → 改实现
5. 查到的事实记进 refs/notes/ 了吗？
```

---

## 测试

改完必须跑（前四个不需要浏览器）：

```powershell
node tools/validate.mjs        # 剧情图 + 数据一致性 + 成长曲线（从剧情图推导）
node tools/reach.mjs           # 可达性：定义了但玩家永远碰不到的内容
node tools/battlesim.mjs       # 把每场战斗真的打完 + 技能机制回归
node tools/balance.mjs         # 数值配平：每场战斗的长度与生存预估

node tools/serve.cjs 8123      # 下面两个需要它
node tools/playthrough.cjs     # 无头浏览器全流程通关（真实路径）
node tools/mobile.cjs          # 6 种视口的布局与真实输入链路
```

改了 `equipment-catalog.json` 必须跑 `node tools/gen-equipment.mjs`，
否则掉落池会静默停在旧数据上，不报任何错。

**测试脚本本身也会过期。** 这个项目已经出现过三次「测试通过但测的是已删除的内容」
（见 TODO.md 的 A7 / A8 / A10）。改剧情后要确认测试断言指向的还是真实存在的东西。

---

## 写提交信息

- 中文，说清楚**为什么**改，不只说改了什么。
- 凡是按原文改的，注明原文第几章 / 哪句话。
- 凡是推测的，明说是推测。
- 结尾带上 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`。
